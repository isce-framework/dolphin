# .readthedocs.yaml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details
version: 2

mkdocs:
  configuration: mkdocs.yml

conda:
  environment: conda-env.yml

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs


# To manually specify the build:
# build:
#   os: "ubuntu-22.04"
#   tools:
#     python: "mambaforge-4.10"
#   commands:
#     - mamba install --file conda-env.yml
#     - mamba install --file docs/requirements.txt --channel conda-forge
#     - pip install --no-deps .
#     - mkdocs build f mkdocs.yml -d _readthedocs/ -W
